export const metadata = {
  title: "Accepter databehandleraftale",
  robots: { index: false, follow: false },
};

export default function AccepterLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
