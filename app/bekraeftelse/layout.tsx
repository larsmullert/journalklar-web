export const metadata = {
  title: "Konto oprettet",
  robots: { index: false, follow: false },
};

export default function BekraeftelseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
